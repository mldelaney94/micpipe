<Window
    x:Class="MicPipe.Views.ImportWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Import clip">

  <Grid Background="{StaticResource SurfaceBrush}" Padding="16" RowSpacing="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Text="Import clip" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />

    <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
      <Button x:Name="FromFileButton" Content="From file" Style="{StaticResource ToolButtonStyle}" Click="FromFile_Click" />
      <Button x:Name="FromUrlButton" Content="From URL" Style="{StaticResource ToolButtonStyle}" Click="FromUrl_Click" />
    </StackPanel>

    <Grid x:Name="UrlPanel" Grid.Row="2" Visibility="Collapsed" RowSpacing="10">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <TextBox x:Name="UrlBox" Header="URL" PlaceholderText="https://..." />
      <Grid Grid.Row="1" ColumnSpacing="8">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="FromBox" Header="From (hh:mm:ss)" PlaceholderText="0:00:00" />
        <TextBox x:Name="ToBox" Grid.Column="1" Header="To (hh:mm:ss)" PlaceholderText="0:00:10" />
      </Grid>
      <TextBox x:Name="NameBox" Grid.Row="2" Header="Name" PlaceholderText="clip name" />
      <TextBlock x:Name="StatusText" Grid.Row="3" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
      <Button Grid.Row="4" Content="Fetch &amp; open trimmer" Style="{StaticResource ToolButtonStyle}" Click="Fetch_Click" />
    </Grid>
  </Grid>
</Window>
