<Window
    x:Class="MicPipe.Views.TrimWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Trim clip">

  <Grid Background="{StaticResource SurfaceBrush}" Padding="16" RowSpacing="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="120" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Text="Trim clip" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
    <TextBlock x:Name="SourceLabel" Grid.Row="1" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />

    <Canvas x:Name="WaveCanvas" Grid.Row="2" Background="{StaticResource SurfaceAltBrush}"
            PointerPressed="Wave_PointerPressed" PointerMoved="Wave_PointerMoved" PointerReleased="Wave_PointerReleased" />

    <Grid Grid.Row="3" ColumnSpacing="8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <TextBlock x:Name="StartLabel" Text="0:00.00" Foreground="{StaticResource TextBrush}" />
      <TextBlock x:Name="SelectionLabel" Grid.Column="1" Text="selection 0:00.00"
                 Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" />
      <TextBlock x:Name="EndLabel" Grid.Column="2" Text="0:00.00" Foreground="{StaticResource TextBrush}"
                 HorizontalAlignment="Right" />
    </Grid>

    <Slider x:Name="StartSlider" Grid.Row="4" Minimum="0" Maximum="1000" Value="0" ValueChanged="Range_Changed" Header="In" />
    <Slider x:Name="EndSlider" Grid.Row="5" Minimum="0" Maximum="1000" Value="1000" ValueChanged="Range_Changed" Header="Out" />

    <Grid Grid.Row="6" ColumnSpacing="8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBox x:Name="NameBox" Header="Name" />
      <Button Grid.Column="1" Content="Play selection" Style="{StaticResource ToolButtonStyle}" Click="Preview_Click" VerticalAlignment="Bottom" />
      <Button Grid.Column="2" Content="Cancel" Style="{StaticResource ToolButtonStyle}" Click="Cancel_Click" VerticalAlignment="Bottom" />
      <Button Grid.Column="3" Content="Save to library" Style="{StaticResource ToolButtonStyle}" Click="Save_Click" VerticalAlignment="Bottom" />
    </Grid>
  </Grid>
</Window>
